image:
  python:3.7

before_script:
  - python3 --version
  - python3 -m venv env
  - source ./env/bin/activate
  - python -m pip install beautifulsoup4 selenium pandas

stages:
  - test

test:
  stage: test
  script:
    - python3 -m gml
